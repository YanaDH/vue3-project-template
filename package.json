{
  "name": "{{ name }}",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "vue3-project-template": "bin/index.js"
  },
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:test": "vue-tsc --noEmit && vite build --mode test",
    "preview:test": "vite preview --mode test",
    "preinstall": "npx only-allow pnpm"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "nprogress": "^0.2.0",
    "pinia": "^2.0.14",
    "vue": "^3.2.25",
    "vue-router": "^4.0.16"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/nprogress": "^0.2.0",
    "@vitejs/plugin-vue": "^2.3.3",
    "sass": "^1.53.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vue-tsc": "^0.34.7"
  },
  "description": "{{ description }}",
  "author": "{{ author }}"
}